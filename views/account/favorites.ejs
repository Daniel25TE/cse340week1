<% if (title) { %>
    <h1>
        <%= title %>
    </h1>
    <% } else { res.redirect('/') } %>

        <% if (message) { %>
            <%- messages() %>
                <% if (errors) { %>
                    <ul class="notice">
                        <% errors.array().forEach(error=> { %>
                            <li>
                                <%= error.msg %>
                            </li>
                            <% }) %>
                    </ul>
                    <% } %>
                        <% } %>


<% if (favorites && favorites.length> 0) { %>
    <form id="favoritesForm" action="/favorites/remove-selected" method="POST">
        <button type="submit" id="removeSelectedBtn">Delete Vehicles</button>
        <div class="client-name-input">
            <label for="clientName">Please type your name:</label>
            <input type="text" id="clientName" name="clientName" placeholder="Please type your name" required>
        </div>
        <section class="favorites-grid">
            <% favorites.forEach(vehicle=> { %>
                <div class="favorite-card">
                    <fieldset>
                        <legend>Check</legend>
                    <label>
                        box
                    <input type="checkbox" name="selected[]" value="<%= vehicle.inv_id %>" class="favorite-checkbox" >
                    </label>
                    </fieldset>
                    <a href="/inv/detail/<%= vehicle.inv_id %>">
                        <img src="<%= vehicle.inv_thumbnail %>" alt="<%= vehicle.inv_make %> <%= vehicle.inv_model %>">
                    </a>
                    <h2>
                        <%= vehicle.inv_make %>
                            <%= vehicle.inv_model %>
                    </h2>
                    <p><strong>Price:</strong> $<%= vehicle.inv_price.toLocaleString() %>
                    </p>
                </div>
                <% }) %>
        </section>
    </form>
    <% } else { %>
        <p>You have no favorites yet. Explore the <a href="/">inventory</a> and add some!</p>
        <% } %>

